import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    pressable: {
      flexDirection: 'row',
      gap: 10,
      alignItems: 'center',
      backgroundColor: '#fff',
      padding: 10
    },
    verificado: {
      width: 35,
      height: 35
    }
  });